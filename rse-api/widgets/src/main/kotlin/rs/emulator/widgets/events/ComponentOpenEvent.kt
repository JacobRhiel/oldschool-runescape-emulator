package rs.emulator.widgets.events

import rs.emulator.widgets.ComponentEvent

/**
 *
 * @author javatar
 */

class ComponentOpenEvent(val parent: Int, val child: Int, val widgetId: Int) : ComponentEvent