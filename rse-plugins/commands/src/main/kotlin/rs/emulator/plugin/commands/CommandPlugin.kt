package rs.emulator.plugin.commands

import org.pf4j.Plugin
import org.pf4j.PluginWrapper

/**
 *
 * @author javatar
 */

class CommandPlugin(wrapper: PluginWrapper) : Plugin(wrapper) {
    override fun start() {
    }
    override fun stop() {
    }
    override fun delete() {
    }
}